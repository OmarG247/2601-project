import React from "react";
import { StyleSheet, View } from "react-native";

const ImageModal = ({ imageData, handleClose }) => (
  <View>
    <View></View>
  </View>
);

const Modal = StyleSheet.create({
  container: {
    height: "100%",
    width: "100%",
    backgroundColor: "rgba(0, 0, 0, 0.25)",
    position: "absolute",
  },
});

export default ImageModal;
